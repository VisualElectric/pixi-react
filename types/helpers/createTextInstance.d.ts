import type { HostConfig } from '../typedefs/HostConfig';
/** Always throws, because we don't support this (yet). */
export declare function createTextInstance(_text: string, _rootContainer: HostConfig['containerInstance'], _hostContext: null, _internalHandle: any): import("../typedefs/PixiReactNode").PixiReactNode<typeof import("pixi.js").Container>;
